import Layout from "@/components/layout";
import GlobeExplorationSection from "@/components/globe-exploration-section";

const GlobeExplorationPage = () => {
  return (
    <Layout footer={false}>
      <GlobeExplorationSection />
    </Layout>
  );
};

export default GlobeExplorationPage;
